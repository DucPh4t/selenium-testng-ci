name: Selenium TLU Parallel Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    name: Test - ${{ matrix.flow }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        flow: [SuccessfulLogin, FailedLogin]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: Run test flow
        env:
          BROWSER: chrome
        run: mvn clean test -Dbrowser=chrome -Dtest=LoginTest#test${{ matrix.flow }}

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports-${{ matrix.flow }}
          path: target/surefire-reports/

      - name: Generate Allure report
        if: always()
        run: mvn allure:report

      - name: Upload Allure report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report-${{ matrix.flow }}
          path: target/site/allure-maven-plugin/
